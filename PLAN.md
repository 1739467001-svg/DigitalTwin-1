# 城市轨交车站数字孪生 · 项目计划

> **一句话定位：把一座地铁站做成可以切开看的活体沙盘。**
> 三维剖面车站 + 实时客流个体 + 按时刻表进出站的列车 + 全天 24 小时高峰推演，配深色工业 HUD。

## 1. 项目定位与优先级

- **第一使命是视觉吸引力**：投屏给一个没看过的人，前 10 秒他会停下来看。孪生的严谨性（接真实数据）是第二阶段的事，本期不做，但仿真内核保持诚实、可替换数据源，为以后留门。
- **作品集定位**：延续既有风格（智慧港口、天府机场、海上油田、会议室孪生）——真实基础设施 + 持续运营数据 + 调度算法 + 深色工业 HUD，补上"城市交通"一块。
- **技术路线**：Three.js 纯前端、零构建依赖、可直接部署 Vercel。依赖已 vendor 进仓库（`vendor/three.module.js` r160 + `OrbitControls.js`）。

## 2. 核心设计判断

**"更真实"不等于"写实渲染"，而是"行为真实 + 电影感光效"。**

纯前端不加重型资产硬追写实材质是死路，做到 70% 写实反而显得廉价。真正让人觉得"真"的是：

1. **行为真实**：列车带加减速曲线对标停车、屏蔽门与车门联动、人群在闸机前排队而不是穿模乱走。观众对"动得对不对"极其敏感，对"贴图精不精"很钝。
2. **电影感光效**：深色场景 + Bloom 辉光 + ACES 色调映射 + 雾效——暗色工业风的"氛围滤镜"。

## 3. 场景与视觉方案

### 3.1 站体：三层剖切结构（Dollhouse Cutaway）

标准岛式站台车站（换乘站复杂度翻倍，留给二期），纵向剖开外墙，一眼看穿三层：

| 层 | 内容 |
|---|---|
| 地面层 | 4 个出入口 + 街道环境示意 |
| 站厅层 | 安检机、售票机、闸机阵列（进/出站分组）、楼扶梯口 |
| 站台层 | 岛式站台 + 两侧轨道 + 屏蔽门 + 候车排队区 |

剖面视角本身是视觉记忆点，且只需建模"里面"，省掉外立面。

### 3.2 客流：低模个体 + 热力图双层表达

- **不做写实人物**：低多边形胶囊小人（微缩沙盘尺度），`InstancedMesh` 单次 draw call 渲染上千人，无骨骼动画，轻微行走起伏即可。按状态着色：进站流 / 出站流 / 站台候车。
- **排队是视觉锚点**：闸机队列、扶梯传送、屏蔽门候车纵队天然有序、观感可信、实现便宜，镜头语言押在这三处。
- **热力图承担宏观叙事**：站厅/站台地面叠客流密度热力图（网格计数 → 模糊 → 单色渐变贴图）。近看个体、远看热力，两层互相掩护。

### 3.3 列车：时刻表驱动

按行车间隔到发（高峰 2.5 分钟 / 平峰 6 分钟），加减速曲线对标停车，车门与屏蔽门联动，乘客按车门位置上下车，停站 30–45 秒。载客率用车厢颜色深浅表达。

### 3.4 视觉冲击力清单（按性价比排序）

1. **Bloom + ACES + 雾效基线**——暗色场景最大单项提升，必须最早做，决定后续所有材质与配色标准。
2. **反光地面**——抛光地面倒影是地铁站场景的灵魂，高光泽材质 + 环境反射，成本低效果暴击。
3. **列车进站"演出"**——全片高潮，单独当功能做：隧道深处头灯光束扫来 → 屏蔽门警示灯闪烁 → 列车呼啸减速进站带轻微镜头震动 → 门联动开启、人流交换。观众看的就是这 15 秒。
4. **导演模式（Demo Loop）**——无操作一段时间后镜头自动运镜：缓慢推轨环绕、切列车进站、跟拍一个乘客从安检到上车。投屏展示不用碰鼠标，自己就是循环短片。**对"吸引/震慑"目标价值最高的单项。**
5. **情绪化状态光效**——大客流红色预警时全场色调向红偏移、告警灯带脉动，数据状态直接变成情绪。
6. **细节动效**——扶梯梯级转动、闸机翼门开合带绿色通过闪光、PIS 屏滚动"下一班 2 分 30 秒"倒计时。
7. **音效（可选）**——列车进站轰鸣、到站提示音、站厅人声底噪，低音量即可，沉浸感翻倍。

**性能红线：全效果 60fps**，Bloom + InstancedMesh 组合在此场景规模下可行，性能预算列入验收。

## 4. 仿真内核（与渲染解耦）

- **到达率曲线**：全天 24h λ(t) 驱动泊松到达，早高峰 7:30–9:00、晚高峰 17:30–19:00 双峰；进站/出站/方向 OD 比例可调。
- **个体状态机**：入口生成 → 安检排队 → 闸机 → 扶梯 → 站台候车区（按车门位置聚簇）→ 容量受限上车；出站流反向。导航图寻路 + 局部分离转向，不用完整社会力模型。
- **固定步长仿真 + 渲染插值**：仿真时钟支持 1×/8×/60× 加速与时间轴拖拽（复刻会议室项目的"全天回放"）；固定随机种子，回放可复现。

## 5. 运营仪表盘（HUD）

| 指标 | 说明 |
|---|---|
| 在站人数 / 进出站速率 | 顶栏核心数字 + 迷你趋势线 |
| 站台密度（人/m²） | 分级预警：<1 舒适 / 1–2 繁忙 / >2.5 红色告警 |
| 列车满载率 / 正点率 | 每列实时，6 编组定员约 1400 人 |
| 平均候车时间 / 留乘率 | "没挤上首班车的比例"，高峰期最有戏的指标 |
| 设备状态 | 闸机、扶梯、屏蔽门逐台健康度 |

## 6. 招牌玩法（自既有作品集迁移）

- **故障注入**（油田同款）：停一台扶梯看客流涌向楼梯、站厅排队暴涨；坏一组闸机看进站速率下跌。
- **限流措施**：站台密度红色预警后，手动/自动"关闭部分进站闸机、扶梯改向"，看密度曲线被压回——真实地铁大客流管控的日常操作。
- **A/B 影子仿真**（天府机场同款）：同一客流种子跑两套策略——"固定行车间隔 vs 动态加开"或"不限流 vs 分级限流"，对比最大站台密度、平均候车时间、留乘率。

## 7. 里程碑计划

| 期 | 功能主线 | 视觉主线 |
|---|---|---|
| **M1** | 剖面站体 + 列车到发 + 单向客流全流程 + 基础 HUD | Bloom/ACES/雾效基线 + 反光地面 + 自发光体系 |
| **M2** | 完整 OD + 全天高峰曲线 + 热力图 + 时间轴回放 | **列车进站演出** + 扶梯/闸机/PIS 细节动效 |
| **M3** | 故障注入 + 密度预警 + 限流措施 | 情绪化告警光效 + **导演模式 Demo Loop** |
| **M4** | A/B 影子仿真对比 | 音效层 + 开场镜头 fly-in |

**每期统一验收标准：**

1. 投屏给一个没看过的人，前 10 秒他会不会停下来看；
2. 全效果稳定 60fps。

## 8. 后续留门（本期不做）

- 换乘站 / 多站线网扩展；
- 接入真实数据源（时刻表 API、闸机流量），从"视觉孪生"升级为真正的数字孪生；
- 项目命名建议：`metro-pulse` / `railtwin`（沿用现有命名风格）。
